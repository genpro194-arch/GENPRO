# Copy to .env and fill values
# Email where reports should be sent
RECIPIENT_EMAIL=yourname@example.com

# SMTP settings (use your provider or Gmail app password + smtp.gmail.com)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password

# Optional
SMTP_FROM="Ruang Aman" <no-reply@ruangaman.local>

# Server port
PORT=3000
